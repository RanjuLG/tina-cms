{
  "name": "tina-cms",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
	"prebuild": "npx @tinacms/cli@latest build",
    "build": "next build",
    "build-local": "tinacms build --local --skip-indexing --skip-cloud-checks && next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.30",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tinacms": "^2.8.0",
    "typescript": "5.5.2"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.10.0",
    "@types/node": "^22.16.0",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.30"
  }
}
